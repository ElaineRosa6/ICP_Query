# Audit fix record — 2026-08-31

This change was made on the existing dirty working tree; unrelated edits were preserved.

## Confirmed issues and changes

- Replaced prefix-only IPv6 classification with validated `IPv6Address.is_global` logic.
- Replaced a timed `Popen.communicate` path that could leave a child process alive with bounded `subprocess.run`.
- Added nine offline regression tests for invalid, private/reserved and global addresses plus command output capture.

## Verification

- `python -m compileall -q utils.py tests` — exit 0
- `python -m pytest -q tests/test_utils.py` — 9 passed, exit 0
